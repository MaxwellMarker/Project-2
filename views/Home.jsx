const React = require('react');
const Layout = require('./components/Layout');

class Home extends React.Component {
    render(){
        return(
            <Layout></Layout>
        )
    }
}

module.exports = Home;